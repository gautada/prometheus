#!/bin/sh
# s6 service: starts prometheus with the default configuration.
# Data is stored in /mnt/volumes/data/prometheus.
exec /usr/bin/prometheus \
    --config.file=/etc/prometheus/config.yaml \
    --storage.tsdb.path=/mnt/volumes/data/prometheus \
    --web.listen-address=0.0.0.0:9090
